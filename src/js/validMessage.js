// const applyValidation = x => {
//   const message = `$Це обов'язкове поле для вводу`;
//   x.setCustomValidity(message);
// };

// function applyValidation(x) {
//   var msg = 'Please enter' + x.name + ' before send';
//   x.setCustomValidity(msg);
// }
